# Vector search tool functions used by agent nodes for RAG retrieval.
# Wraps the RAG retrieval pipeline: hybrid dense+sparse search → rerank → context assemble.
# Used by: ProductRecAgent (product eligibility), OutreachGenAgent (persona tone),
# RMCopilotAgent (ad-hoc Q&A), and ExplainabilityAgent (policy grounding).

# TODO: implement vector tool functions in Phase 6 (RAG layer)
